import React from 'react';

const Footer = () => {
    return (
        <footer>
            <p>&copy; {new Date().getFullYear()} onTech. All rights reserved.</p>
        </footer>
    );
};

export default Footer;
